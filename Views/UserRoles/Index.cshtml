@model IEnumerable<M3LMS.Models.ref_UserRoles>

<!-- Dropdown Plugin -->
<script src="~/Content/css/dropdown/all.js"></script>
<link href="~/Content/css/dropdown/jquery.multiselect.css" rel="stylesheet" />
<script src="~/Content/css/dropdown/jquery.multiselect.js"></script>
<script src="~/Content/css/dropdown/platform.js"></script>
<link href="~/Content/css/dropdown/style.css" rel="stylesheet" />
<script>
    $('#langOpt3').multiselect({ columns: 1, placeholder: 'Select Languages', search: true, selectAll: true });
</script>

<!-- Alert -->


<script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.1/jquery-ui.min.js"></script>
<link href="~/Content/css/popup/alert.min.css" rel="stylesheet" />
<script src="~/Content/css/popup/alert.min.js"></script>
<link href="~/Content/css/popup/theme.min.css" rel="stylesheet" />

<style>
    .ms-options ul {
    margin:0;
    padding:0;
    list-style:none
}
.ms-options ul label{
    padding-left:20px !important
}
</style>

<div class="panel">
        <div class="panel-body">
            <div class="page-heading">
                <h2 class="pull-left">User Roles</h2>
            </div>
            <p>We Are working on this model. </p>
      </div>

</div>

<p>
    @*@Ajax.ActionLink("Assign Role", "Create", "UserRoles", new AjaxOptions { HttpMethod = "GET", UpdateTargetId = "content_wrpr", InsertionMode = InsertionMode.Replace })*@
</p>

    @*
    @foreach (var item in Model)
    {

        M3LMS.Models.DB_A14BC3_M3LMSEntities2 db = new M3LMS.Models.DB_A14BC3_M3LMSEntities2();
        var DeptName = db.ref_department.Where(a => a.DeptId == item.DeptId).Select(a => a.DeptName).SingleOrDefault();
        var RoleName = db.ref_Roles.Where(a => a.RoleId == item.RoleId).Select(a => a.RoleName).SingleOrDefault();

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>

                @DeptName
            </td>
            <td>
                @RoleName
            </td>
            <td >
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }
    *@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

}